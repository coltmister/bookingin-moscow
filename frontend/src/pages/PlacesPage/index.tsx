import { RootLayout } from '@/layouts';
import { Catalogue } from '@/widgets';

export const PlacesPage = () => {
    return (
        <RootLayout>
            <Catalogue />
        </RootLayout>
    );
};
