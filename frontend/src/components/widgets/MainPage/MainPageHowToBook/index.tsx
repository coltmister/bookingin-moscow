import { HowTo } from '@/components';

import s from '../styles.module.scss';

export const MainPageHowToBook = () => {
    return (
        <section className={s.section}>
            <HowTo />
        </section>
    );
};
